diff --git a/js/misc/inputMethod.js b/js/misc/inputMethod.js
index 64bcb8e09..53ef903b4 100644
--- a/js/misc/inputMethod.js
+++ b/js/misc/inputMethod.js
@@ -33,6 +33,10 @@ export const InputMethod = GObject.registerClass({
         this._preeditVisible = false;
         this._hidePanelId = 0;
         this._surroundingText = null;
+        // fajita: committed-text tail for synchronous auto-cap decisions
+        // null = focus just entered a new field (treat as sentence start)
+        // 'a'  = sentinel: known-not-sentence-start (e.g. after backspace)
+        this._committedTail = null;
         this._surroundingTextCursor = null;
         this._surroundingTextAnchor = null;
         this._ibus = IBus.Bus.new_async();
@@ -121,7 +125,30 @@ export const InputMethod = GObject.registerClass({
     }
 
     _onCommitText(_context, text) {
-        this.commit(text.get_text());
+        const str = text.get_text();
+        this.commit(str);
+        // fajita: track the last 8 chars of committed text so keyboard.js
+        // _updateLevelFromHints can make a synchronous auto-cap decision
+        // without waiting for surrounding-text (which never arrives on this
+        // stack: GTK4→mutter→shell delivers nothing).
+        this._committedTail = ((this._committedTail ?? '') + str).slice(-8);
+    }
+
+    // null  = fresh field / sentence start → shift
+    // 'a'   = sentinel after backspace → no-shift (cursor position unknown)
+    // other = tail of committed text → shift iff ends [.!?¡¿…]+\s*
+    get committedTail() {
+        return this._committedTail;
+    }
+
+    // Call when backspace is pressed: we no longer know the tail.
+    invalidateCommittedTail() {
+        this._committedTail = 'a';
+    }
+
+    // Call when Return/Enter is pressed: next char is a sentence start.
+    noteNewline() {
+        this._committedTail = null;
     }
 
     _onRequireSurroundingText(_context) {
@@ -194,6 +221,8 @@ export const InputMethod = GObject.registerClass({
 
     vfunc_focus_in(focus) {
         this._currentFocus = focus;
+        // fajita: new field = unknown tail; treat as sentence start
+        this._committedTail = null;
         if (this._context) {
             this.update();
             this._context.focus_in();
diff --git a/js/ui/keyboard.js b/js/ui/keyboard.js
index 08d0a8e36..ad8480315 100644
--- a/js/ui/keyboard.js
+++ b/js/ui/keyboard.js
@@ -1895,7 +1895,7 @@ export const Keyboard = GObject.registerClass({
         this._updateLevelFromHints(false);
     }
 
-    _updateLevelFromHints(userInputHappened) {
+    _updateLevelFromHints(userInputHappened, pendingCommit = '') {
         // If the latch is enabled, avoid level changes
         if (this._latched)
             return;
@@ -1916,36 +1916,33 @@ export const Keyboard = GObject.registerClass({
         if ((this._contentHint &
              (Clutter.InputContentHintFlags.AUTO_CAPITALIZATION |
               Clutter.InputContentHintFlags.TITLECASE)) !== 0) {
-            if (this._surroundingTextId)
-                return;
-
-            this._surroundingTextId =
-                Main.inputMethod.connect('surrounding-text-set', () => {
-                    const [text, cursor] = Main.inputMethod.getSurroundingText();
-                    if (!text || cursor === 0) {
-                        // First character in the buffer
-                        this._setActiveLevel('shift');
-                        return;
-                    }
-
-                    const beforeCursor = GLib.utf8_substring(text, 0, cursor);
-
-                    if ((this._contentHint & Clutter.InputContentHintFlags.TITLECASE) !== 0) {
-                        if (beforeCursor.charAt(beforeCursor.length - 1) === ' ')
-                            this._setActiveLevel('shift');
-                        else
-                            this._setActiveLevel('default');
-                    } else if ((this._contentHint & Clutter.InputContentHintFlags.AUTO_CAPITALIZATION) !== 0) {
-                        if (beforeCursor.charAt(beforeCursor.trimEnd().length - 1) === '.')
-                            this._setActiveLevel('shift');
-                        else
-                            this._setActiveLevel('default');
-                    }
-
-                    Main.inputMethod.disconnect(this._surroundingTextId);
-                    this._surroundingTextId = 0;
-                });
-            Main.inputMethod.request_surrounding();
+            // fajita: surrounding text never arrives on this stack
+            // (GTK4→mutter→shell delivers nothing — Task-1 context).
+            // Decide synchronously from the committed-text tail tracked in
+            // inputMethod.js _onCommitText instead. pendingCommit is text
+            // the OSK is about to commit locally (key release): folding it
+            // in drops the shift level in the SAME tick as the keypress
+            // instead of after the engine D-Bus round trip (which takes
+            // hundreds of ms under load — fast typing produced "WHat").
+            let tail = Main.inputMethod.committedTail;
+            if (pendingCommit)
+                tail = (tail ?? '') + pendingCommit;
+
+            if ((this._contentHint & Clutter.InputContentHintFlags.TITLECASE) !== 0) {
+                // TITLECASE: shift after any space (start of each word)
+                if (tail === null || tail === '' ||
+                    tail.charAt(tail.length - 1) === ' ')
+                    this._setActiveLevel('shift');
+                else
+                    this._setActiveLevel('default');
+            } else {
+                // AUTO_CAPITALIZATION: shift at sentence start or after [.!?¡¿…]
+                if (tail === null || tail === '' ||
+                    /[.!?¡¿…]\s*$/.test(tail))
+                    this._setActiveLevel('shift');
+                else
+                    this._setActiveLevel('default');
+            }
             return;
         }
 
@@ -2164,6 +2161,9 @@ export const Keyboard = GObject.registerClass({
                     } else if (key.action === 'delete') {
                         const alien = this._focusWindow?.is_alien();
 
+                        // fajita: after a delete we can't know the tail;
+                        // sentinel to avoid false-shifting after a backspace.
+                        Main.inputMethod.invalidateCommittedTail();
                         this._keyboardController.toggleDelete(true, alien);
                         this._keyboardController.toggleDelete(false, alien);
                         this._updateLevelFromHints(true);
@@ -2196,6 +2196,9 @@ export const Keyboard = GObject.registerClass({
 
                     this._keyboardController.keyvalPress(key.keyval);
                     this._keyboardController.keyvalRelease(key.keyval);
+                    // fajita: Return starts a new line → treat as sentence start
+                    if (key.keyval === '0xff0d')
+                        Main.inputMethod.noteNewline();
                     this._updateLevelFromHints(true);
                 });
                 button.connect('cancelled', (_actor) => {
@@ -2206,6 +2209,13 @@ export const Keyboard = GObject.registerClass({
                 });
             } else {
                 button.connect('commit', (_actor, str) => {
+                    // fajita: re-evaluate the shift level in the SAME tick
+                    // as the local commit (Android drops shift on the key
+                    // tap itself); the .then() below re-evaluates again
+                    // with authoritative data once the engine round trip
+                    // lands. Without this the level lagged ~0.5 s and fast
+                    // typing produced "WHat".
+                    this._updateLevelFromHints(true, str);
                     this._keyboardController.commit(str, this._modifiers).then(() => {
                         this._disableAllModifiers();
                         this._updateLevelFromHints(true);

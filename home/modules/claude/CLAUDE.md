# Claude usage budget

Claude usage is limited; the Codex subscription and cheap OpenRouter models are far cheaper (`offload mode` picks which). For token-heavy, easy-to-verify work — exploring unfamiliar code, reading docs or web pages, triaging long logs, reviewing a diff — use the `offload` and `cheap-review` skills before spending your own tokens, and prefer them over Claude subagents for that work. Keep design, tricky debugging, and final verification on Claude.
